const handler = require('./handler')

describe('OAuth handler for Slack', function() {

  describe('main', () => {
    it('should handle request with `code` parameter in body and send OAuth request to Slack')
    it('should return error callback when `code` parameter not found in body')
  })

})
